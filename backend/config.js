export const PORT = 5555;
export const mongoDBURL = "mongodb://localhost:27017/admin";
// we added a json file here for our data just to make sure you can run because it is on 
// Mongo Compass and we do not have cloud database